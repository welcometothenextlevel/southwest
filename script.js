const menu=document.querySelector('.menu'),nav=document.querySelector('header nav');function closeMenu(){menu.setAttribute('aria-expanded','false');nav.classList.remove('open');document.body.classList.remove('menu-open')}menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open);document.body.classList.toggle('menu-open',!open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));
const carousel=document.querySelector('.carousel'),track=document.querySelector('.track'),slides=[...track.children],dotsBox=document.querySelector('.dots'),counter=document.querySelector('.progress strong');let current=0,timer,startX=0;slides.forEach((_,i)=>{const b=document.createElement('button');b.className='dot';b.setAttribute('aria-label',`Show photo ${i+1}`);b.addEventListener('click',()=>{show(i);restart()});dotsBox.appendChild(b)});const dots=[...dotsBox.children];function show(i){current=(i+slides.length)%slides.length;const gap=parseFloat(getComputedStyle(track).gap)||0;track.style.transform=`translateX(-${current*(slides[0].getBoundingClientRect().width+gap)}px)`;slides.forEach((s,n)=>s.classList.toggle('active',n===current));dots.forEach((d,n)=>d.classList.toggle('active',n===current));counter.textContent=String(current+1).padStart(2,'0')}function restart(){clearInterval(timer);if(!matchMedia('(prefers-reduced-motion: reduce)').matches)timer=setInterval(()=>show(current+1),5000)}document.querySelector('.prev').onclick=()=>{show(current-1);restart()};document.querySelector('.next').onclick=()=>{show(current+1);restart()};carousel.addEventListener('touchstart',e=>startX=e.touches[0].clientX,{passive:true});carousel.addEventListener('touchend',e=>{const d=e.changedTouches[0].clientX-startX;if(Math.abs(d)>45)show(current+(d<0?1:-1));restart()},{passive:true});window.addEventListener('resize',()=>show(current));show(0);restart();
document.querySelector('#quote-form').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget),s=encodeURIComponent(`Free quote request from ${d.get('name')}`),b=encodeURIComponent(`Name: ${d.get('name')}\nPhone: ${d.get('phone')}\n\nJob details:\n${d.get('message')}`);location.href=`mailto:Glassandwardroves@gmail.com?subject=${s}&body=${b}`});document.querySelector('#year').textContent=new Date().getFullYear();
